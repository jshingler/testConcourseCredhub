---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: ((JIM))
    tag: latest

inputs:
  - name: source-code

outputs:
  - name: build

run:
  path: echo
  args: ["Hello, JAVA!"]
